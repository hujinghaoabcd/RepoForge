# Frontend Library — Minimal Profile

Use Minimal for a small, focused package with one primary install target and a compact public API.

Expected sections:

- library identity;
- Install;
- Quick Start / Usage;
- Browser or runtime support;
- License.

A required CSS import or peer dependency may appear beside the first example. Do not add API catalogues, framework matrices, bundle-analysis sections, or release policy unless they materially affect a small package's use.
